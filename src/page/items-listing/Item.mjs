// view model for UI logic
// all model files extension should be `.mjs` for easy to differentiate

export class Item {
    constructor() {
        // example
    }

    intro() {
        console.log(' i am ' + typeof (this));
    }
}